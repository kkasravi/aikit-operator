# Use the 'create api' subcommand to add watches to this file.
- group: operators.kubernetes.orchestrators.containers.intel.com
  version: v1alpha1
  kind: AIKitOperator
  chart: helm-charts/aikitoperator
# +kubebuilder:scaffold:watch
